package ru.yandex.tasks;

public class Node {
    /*
     * Класс для односвязного списка
     * value - значение, -10^5 <= value <= 10^5
     * next - следующий элемент
     * Можно менять под свои нужды, value и next не трогаем :)
     */
    public int value;
    public Node next;
}
